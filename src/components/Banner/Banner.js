import './Banner.css'

export const Banner = () => {
    return (
        // JSX - Como o react trata os elementos HTML. 
        <header className="banner">
            <img src="/imagens/banner.png" alt="O banner principal da página do organo"></img>
        </header>
    )
}