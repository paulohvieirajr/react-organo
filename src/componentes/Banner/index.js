import './Banner.css'

function Banner() {
    // JSX: parece HTML, mas não é. É como o react trabalha com HTML.
    return (
        <header className="banner">
            <img src="/imagens/banner.png" alt="Banner principal da página do Organo"/>
        </header>
    )
}

export default Banner